# Copy this file to `.env` and fill in values as needed.

# Optional: enable LLM evaluation for quality checking.
# Get an API key at: https://platform.openai.com/
OPENAI_API_KEY=

# Optional: override default model used for LLM evaluation.
# Example: gpt-4o-mini, gpt-4o, etc.
WEBTOMD_LLM_MODEL=gpt-4o-mini

# Firecrawl API key (optional) for --use-firecrawl or as a fallback.
# Get your key from: https://app.firecrawl.dev/ (see docs: https://docs.firecrawl.dev/)
# IMPORTANT: do not commit real secrets to source control.
FIRECRAWL_API_KEY=

# Jina Reader v1 requires no API key.
# We call https://r.jina.ai/{http|https}://... directly when using --use-jina.
